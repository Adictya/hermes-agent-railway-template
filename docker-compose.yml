services:
  hermes:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8081
      - PROXY_PORT=8080
      - API_SERVER_PORT=8642
      - WEBHOOK_PORT=8644
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
    volumes:
      - hermes-data:/data

volumes:
  hermes-data:
